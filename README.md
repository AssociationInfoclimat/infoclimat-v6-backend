## Nestjs API

```bash
cd api
# -> ./nestjs-backend/README.md
```

## Database

Using the root docker-compose.yml:

```bash
mkdir ./data # if ./data does not exist
docker compose up
```

## Mapserver

-> mapserver/README.md
