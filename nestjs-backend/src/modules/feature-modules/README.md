# About feature modules

- Each feature module imports one or several entity-modules to build a feature.
- A feature module does not have any repository. The repositories are for entity modules (1 entity module = 1 db table).

See ../entity-modules/README.md
