# About our modules

- Entity modules (entity-module/README.md): Independent module (1 module = 1 db table)
- Feature modules (feature-modules/README.md): They use one or several entity modules to build features
- CRON modules
